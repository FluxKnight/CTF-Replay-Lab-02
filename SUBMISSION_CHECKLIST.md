# Stardance Submission Checklist

1. Replace the old deployed files with every file from this folder.
2. Confirm `index.html` loads `style.css?v=400` and `app.js?v=400`.
3. Hard-refresh the deployed page with `Ctrl + Shift + R`.
4. Move the mouse over the main page and open Author Manifest; the native cursor must remain visible.
5. Click Demo and complete the Ghost Tail flow through verified flag.
6. Open System Check and confirm **0 failed** checks.
7. Test the deployed URL once at desktop width and once on a phone.
8. Record the re-certification video showing:
   - landing-page identity
   - Signature Map
   - Byte Intelligence Map
   - archive extraction and Auto-probe
   - successful manifest verification
   - Evidence Constellation
   - System Check with no failed checks
9. Paste the text from `RECERTIFICATION_MESSAGE.txt` into the submission notes.
10. Request re-certification only after the deployed URL shows this v4 build.
